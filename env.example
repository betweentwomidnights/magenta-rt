# optional environment variables for model configuration
# if not set, the backend will use the stock magenta-rt base/large model

# huggingface repo containing your finetune checkpoints
# example: thepatch/magenta-ft
# MRT_CKPT_REPO=

# checkpoint step number to load on startup
# this should match one of the checkpoint_XXXXXX.tgz files in your repo
# example: 1863001
# MRT_CKPT_STEP=

# base model size: "base" or "large"
# this should match which base model you finetuned from
# default: "large"
# MRT_SIZE=large

# usage example with docker:
# docker run -it --rm --gpus all --shm-size=16g -p 7860:7860 \
#   -e MRT_CKPT_REPO="thepatch/magenta-ft" \
#   -e MRT_CKPT_STEP="1863001" \
#   -e MRT_SIZE="large" \
#   magenta-rt:latest

# or create a .env file and use --env-file:
# docker run -it --rm --gpus all --shm-size=16g -p 7860:7860 \
#   --env-file .env \
#   magenta-rt:latest
